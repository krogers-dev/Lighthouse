EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_CLIENT_KEY=
# Optional. The firm's support address, shown as a tappable line in Help and
# Account. Public information only; reserved/testing domains are refused
# outside development. Leave empty until a real address is confirmed.
EXPO_PUBLIC_SUPPORT_EMAIL=
